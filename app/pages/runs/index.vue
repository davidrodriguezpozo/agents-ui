<script setup lang="ts">
/**
 * Merged into Work. Runs are no longer a separate list — a session is one row
 * however many turns it took, and a ritual or command run sits beside it.
 */
await navigateTo({ path: '/work' }, { replace: true })
</script>
