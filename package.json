{
  "name": "agents-studio",
  "version": "0.5.2",
  "type": "module",
  "description": "Leave Claude Code running — work that fires on a schedule against your own repositories, checks itself with your own tests, and stops when it can't.",
  "keywords": [
    "claude",
    "claude-code",
    "agents",
    "git-worktree",
    "parallel",
    "ai"
  ],
  "license": "MIT",
  "author": "David Rodriguez Pozo",
  "homepage": "https://github.com/davidrodriguezpozo/agents-ui#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/davidrodriguezpozo/agents-ui.git"
  },
  "bugs": {
    "url": "https://github.com/davidrodriguezpozo/agents-ui/issues"
  },
  "bin": {
    "agents-studio": "bin/start.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "//": "Only the built server and its launcher ship. The build is self-contained — Nitro vendors dependencies into .output/server/node_modules — so an install has nothing to resolve and nothing to compile.",
  "files": [
    ".output",
    "bin",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "typecheck": "nuxt typecheck",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepare": "nuxt prepare",
    "prepublishOnly": "npm run build",
    "prepack": "node scripts/dereference-output.mjs"
  },
  "//dependencies": "There are deliberately none. Nitro vendors what the server needs into .output/server/node_modules, so a published install resolves nothing, compiles nothing, and does not drag Nuxt and the whole build toolchain onto a machine that only wants to run the thing. Everything below is needed to build it, which makes it a dev dependency by definition. `prepare` rather than `postinstall` for the same reason: postinstall would run on a user's machine and fail looking for nuxt.",
  "devDependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.232",
    "@iconify-json/lucide": "^1.2.95",
    "@nuxt/ui": "^3",
    "@types/node": "^26.1.2",
    "@vue-flow/controls": "^1.1",
    "@vue-flow/core": "^1.41",
    "@vue-flow/minimap": "^1.5",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "marked": "^17.0.4",
    "nuxt": "^3.16",
    "typescript": "^5.7",
    "vitest": "^4.1.10",
    "vue-tsc": "^3.3.9",
    "yaml": "^2.6"
  }
}
